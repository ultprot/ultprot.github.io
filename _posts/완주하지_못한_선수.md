---
categories:
    - Algorithm
---
# 완주하지 못한 선수
## 두 배열 정렬하고 반복문으로 비교
### C++
```cpp
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

string solution(
    vector<string> participant, 
    vector<string> completion
)
{
    sort(participant.begin(), participant.end());   //참가자 명단 정렬
    sort(completion.begin(), completion.end()); //완주한 선수 명단 정렬
    string answer = "";
    int end = participant.size();
    for(int i=0;i<end;i++){
        if(i==end-1){   
            //마지막 참가자 제외하고 모두 확인한 경우
            answer = participant[i];    
            //마지막 참가자가 완주하지 못했다.
            break;
        }
        if(participant[i]!=completion[i]){  
            //같은 인덱스의 참가자와 완주한 선수가 다른 경우
            answer = participant[i];
            //현재 참가자가 완주하지 못했다.
            break;
        }
    }
    return answer;
}
```

### java
```java
import java.util.Arrays;

class Solution {
    public String solution(String[] participant, String[] completion) {
        String answer = "";
        Arrays.sort(participant);
        Arrays.sort(completion);
        for(int i=0;i<participant.length;i++){
            if(i == participant.length-1){
                answer = participant[i];
                break;
            }else{
                if(!participant[i].equals(completion[i])){
                    answer = participant[i];
                    break;
                }
            }
        }
        return answer;
    }
}
```

### Python
```python
def func(participants, completion):
    
```
- 해시맵 이용
```java
public class Fruit implements Comparable<Fruit>{
    public String name;
    public int quantity;
    public Fruit(String name, int quantity){
        this.name = name;
        this.quantity = quantity;
    }
    public int compareTo(Fruit other){
        return name.compareTo(other.name);
    }
    public static Compare<Fruit> nameComparator = 
        new Comparator<Fruit>(){
            public int compare(Fruit fruit1, Fruit fruit2){
                return fruit1.name.compareTo(fruit2.name);
            }
        };
    public static Compare<Fruit> quantComparator = 
        new Comparator<Fruit>(){
            public int compare(Fruit fruit1, Fruit fruit2){
                return fruit1.quantity - fruit2.quantity;
            }
        }
}

```